# Dev / demo deps for dev.py (runs on any OS, no hardware).
# Everything from requirements.txt minus the Pi-5-only Piomatter wheel and
# truthbrush/readchar, which dev mode doesn't use.  pyserial is kept because
# flagpole.py transitively imports arduino.py which imports serial at the top
# level, even though dev mode substitutes a FakeArduino.

Pillow>=10
numpy>=1.24
python-dateutil>=2.8
python-dotenv>=1.0
pyserial>=3.5
